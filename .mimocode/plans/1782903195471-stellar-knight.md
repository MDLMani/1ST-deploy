# Frontend Implementation Plan: Modern Ticket System UI

## Overview
Apply 11 modern ticket system features to TVK-ADMIN and TVK-USER frontend portals. Both are React 19 + TypeScript + Vite + Tailwind CSS 4 apps with Zustand + React Query + Socket.IO.

---

## Shared Changes (Both Projects)

### 1. Update Constants

**TVK-ADMIN** (`/home/mdlmani/TVK/TVK-ADMIN/src/constants/index.ts`):
- Add `PENDING`, `MERGED` to `TicketStatus`
- Add `Department`, `AssignmentStrategy`, `EscalationTrigger`, `SatisfactionRating` enums
- Add new `SOCKET_EVENTS`: `ticketMerged`, `slaBreach`, `slaWarning`, `ticketEscalated`, `internalNoteAdded`, `satisfactionSubmitted`
- Add `DEPARTMENTS` array constant

**TVK-USER** (`/home/mdlmani/TVK/TVK-USER/src/constants/tickets.ts`):
- Same `TicketStatus` updates
- Add `DEPARTMENTS` array constant
- Add new `SOCKET_EVENTS`

### 2. Update TypeScript Types

**TVK-ADMIN** (`/home/mdlmani/TVK/TVK-ADMIN/src/types/index.ts`):
```typescript
// Update Ticket interface - add:
department?: Department;
tags: Tag[];
isInternal: boolean;
mergedInto?: string;
relatedTickets: string[];
customFields: Record<string, any>;
firstResponseAt?: string;
resolvedAt?: string;
sla?: {
  responseDeadline?: string;
  resolutionDeadline?: string;
  responseBreached: boolean;
  resolutionBreached: boolean;
};
escalationLevel: number;
knowledgeBaseLinks: string[];

// Update Comment interface - add:
isInternal: boolean;

// Add new interfaces:
interface Department { _id: string; name: string; slug: string; description?: string; defaultPriority: string; assignmentStrategy: string; slaPolicy: any; isActive: boolean; }
interface Tag { _id: string; name: string; color: string; usageCount: number; }
interface CannedResponse { _id: string; title: string; content: string; shortcut: string; category: string; isGlobal: boolean; usageCount: number; }
interface KnowledgeBase { _id: string; title: string; slug: string; content: string; category: string; tags: string[]; viewCount: number; helpfulCount: number; notHelpfulCount: number; isPublished: boolean; }
interface CustomField { _id: string; name: string; key: string; type: string; options?: string[]; isRequired: boolean; defaultValue?: string; displayOrder: number; }
interface EscalationRule { _id: string; name: string; trigger: string; conditions: any; actions: any; isActive: boolean; }
interface Satisfaction { _id: string; ticket: string; rating: number; comment?: string; createdAt: string; }
interface SLAStatus { hasSLA: boolean; responseDeadline?: string; resolutionDeadline?: string; responseBreached: boolean; resolutionBreached: boolean; responseRemaining?: number; resolutionRemaining?: number; }
interface CSATStats { totalResponses: number; averageRating: number; distribution: { rating: number; count: number; percentage: number }[]; trends: { month: string; avg: number; count: number }[]; }
```

**TVK-USER** (`/home/mdlmani/TVK/TVK-USER/src/types/index.ts`):
- Same Ticket interface updates (department, tags, sla, etc.)
- Update Comment interface (add `isInternal`)
- Add Department, Tag, SLAStatus, Satisfaction interfaces
- Update CreateTicketInput (add department, tags, customFields)

---

## TVK-ADMIN Changes

### Phase 1: Services Layer

**Update `src/services/ticket.service.ts`** - Add new API methods:
```typescript
// Departments
getAll: async (filters) => { /* existing */ },
getByDepartment: async (deptId, filters) => { ... },

// SLA
getSLAStatus: async (id) => { ... },

// Merge
mergeTickets: async (sourceIds, targetId) => { ... },
linkRelated: async (ticketId, relatedIds) => { ... },
unlinkRelated: async (ticketId, relatedId) => { ... },

// Internal Notes
addInternalNote: async (ticketId, message) => { ... },
getComments: async (ticketId, includeInternal?) => { ... },
```

**Create `src/services/department.service.ts`**:
```typescript
getAll, getById, create, update, delete
```

**Create `src/services/tag.service.ts`**:
```typescript
getAll, getPopular, create, update, delete
```

**Create `src/services/knowledgeBase.service.ts`**:
```typescript
getAll, search, getBySlug, voteHelpful, create, update, delete
```

**Create `src/services/customField.service.ts`**:
```typescript
getAll, create, update, delete, reorder
```

**Create `src/services/escalation.service.ts`**:
```typescript
getAll, create, update, delete
```

**Create `src/services/satisfaction.service.ts`**:
```typescript
getTicketRating, getCSATStats
```

**Create `src/services/cannedResponse.service.ts`**:
```typescript
getAll, getByShortcut, create, update, delete
```

### Phase 2: React Query Hooks

**Update `src/hooks/useTickets.ts`** - Add new hooks:
```typescript
// Query keys
ticketKeys.sla(id) = [...ticketKeys.all, 'sla', id]
ticketKeys.merge = ['merge']

// New hooks
useTicketSLAStatus(id)
useMergeTickets()
useLinkRelated(ticketId)
useUnlinkRelated(ticketId)
useAddInternalNote(ticketId)
```

**Create `src/hooks/useDepartments.ts`**:
```typescript
useDepartments, useCreateDepartment, useUpdateDepartment, useDeleteDepartment
```

**Create `src/hooks/useTags.ts`**:
```typescript
useTags(filters), usePopularTags, useCreateTag, useUpdateTag, useDeleteTag
```

**Create `src/hooks/useKnowledgeBase.ts`**:
```typescript
useArticles(filters), useSearchArticles(query), useArticle(slug), useVoteHelpful, useCreateArticle, useUpdateArticle, useDeleteArticle
```

**Create `src/hooks/useCustomFields.ts`**:
```typescript
useCustomFields(deptId), useCreateCustomField, useUpdateCustomField, useDeleteCustomField, useReorderCustomFields
```

**Create `src/hooks/useEscalation.ts`**:
```typescript
useEscalationRules, useCreateRule, useUpdateRule, useDeleteRule
```

**Create `src/hooks/useSatisfaction.ts`**:
```typescript
useTicketRating(ticketId), useCSATStats
```

### Phase 3: New Pages

**Create `src/pages/DepartmentsPage.tsx`**:
- Table/list of departments with CRUD
- Modal form for create/edit (name, slug, description, SLA policy, assignment strategy)
- Delete confirmation

**Create `src/pages/TagsPage.tsx`**:
- Grid/list of tags with usage counts
- Create/edit/delete with color picker
- Popular tags section

**Create `src/pages/KnowledgeBasePage.tsx`**:
- Article list with search, category filter
- Article detail view (markdown content)
- Create/edit form with title, slug, content, category, tags
- Vote helpful/not helpful buttons

**Create `src/pages/EscalationRulesPage.tsx`**:
- Rules table with trigger type, conditions, actions
- Create/edit form with condition builder
- Toggle active/inactive

**Create `src/pages/CSATStatsPage.tsx`**:
- Average rating display
- Rating distribution chart
- Trend chart (monthly avg)
- Recent ratings list

### Phase 4: Update Existing Pages

**Update `src/pages/TicketsPage.tsx`**:
- Add department filter dropdown
- Add tag filter (multi-select)
- Add date range filter
- Add "Merge Tickets" action (select multiple → merge)
- Add merge button in toolbar

**Update `src/pages/TicketDetailPage.tsx`**:
- Add SLA status card (response/resolution deadlines, breach indicators)
- Add department and tags display
- Add internal notes section (staff-only, styled differently)
- Add related tickets section (linked tickets list)
- Add "Link Related Ticket" button
- Add custom fields display
- Update status options to include PENDING, MERGED
- Add satisfaction rating display (if rated)

**Update `src/pages/DashboardPage.tsx`**:
- Add department breakdown chart
- Add SLA breach count stat card
- Add CSAT average stat card

**Update `src/components/layout/Sidebar.tsx`**:
- Add nav items: Departments, Tags, Knowledge Base, Escalation Rules, CSAT

**Update `src/routes/index.tsx`**:
- Add routes: `/departments`, `/tags`, `/knowledge-base`, `/escalation-rules`, `/csat`

**Update `src/components/tables/TicketTable.tsx`**:
- Add department column
- Add tags column
- Add SLA status column

---

## TVK-USER Changes

### Phase 1: Services & Hooks

**Update `src/services/ticket/ticket.service.ts`**:
```typescript
// Add methods:
getSLAStatus: async (id) => { ... },
rateTicket: async (id, rating, comment?) => { ... },
getTicketRating: async (id) => { ... },
searchKB: async (query) => { ... },
getSuggestedArticles: async (category, tags) => { ... },
```

**Update `src/hooks/useTickets.ts`**:
```typescript
// Add hooks:
useTicketSLAStatus(id)
useRateTicket(ticketId)
useTicketRating(ticketId)
useSearchKB(query)
useSuggestedArticles(category, tags)
```

### Phase 2: Update Pages

**Update `src/pages/submit-ticket/submit-ticket.tsx`**:
- Add department dropdown (fetch from `/departments`)
- Add tags multi-select
- Show suggested KB articles as user types
- Pass department, tags to create endpoint

**Update `src/pages/ticket-detail/ticket-detail.tsx`**:
- Add SLA status card (deadline countdown, breach warnings)
- Add "Rate This Ticket" section (after resolution, 1-5 stars)
- Add satisfaction rating display
- Add KB article suggestions related to ticket
- Show department and tags

**Update `src/pages/live-updates/live-updates.tsx`**:
- Add department filter
- Add status filter (expanded with PENDING)
- Show SLA status on ticket cards
- Show tags on ticket cards

**Update `src/sockets/index.ts`**:
- Add handlers for `slaBreach`, `slaWarning`, `ticketEscalated`, `satisfactionSubmitted`
- Add `includeInternal` support for comments

**Update `src/constants/tickets.ts`**:
- Add `PENDING`, `MERGED` to TicketStatus
- Add new socket events
- Add departments list

---

## Implementation Order

| Step | Project | What | Files |
|------|---------|------|-------|
| 1 | Both | Update constants | `constants/index.ts`, `constants/tickets.ts` |
| 2 | Both | Update TypeScript types | `types/index.ts` |
| 3 | ADMIN | Create service files (7 new) | `services/*.service.ts` |
| 4 | ADMIN | Create hook files (6 new) | `hooks/*.ts` |
| 5 | ADMIN | Update ticket service + hooks | `services/ticket.service.ts`, `hooks/useTickets.ts` |
| 6 | ADMIN | Create new pages (5) | `pages/*Page.tsx` |
| 7 | ADMIN | Update existing pages (4) | `pages/TicketsPage`, `TicketDetailPage`, `DashboardPage` |
| 8 | ADMIN | Update sidebar + routes | `Sidebar.tsx`, `routes/index.tsx` |
| 9 | USER | Update ticket service + hooks | `services/ticket.service.ts`, `hooks/useTickets.ts` |
| 10 | USER | Update pages (3) | `submit-ticket`, `ticket-detail`, `live-updates` |
| 11 | USER | Update sockets + constants | `sockets/index.ts`, `constants/tickets.ts` |

---

## Verification

1. **TypeScript**: Run `npx tsc --noEmit` in both projects — should pass
2. **Dev server**: Run `npm run dev` in both projects — should start without errors
3. **API integration**: Test new endpoints from browser dev tools
4. **Visual check**: Navigate all new pages, verify UI renders correctly
5. **Real-time**: Verify socket events trigger UI updates
